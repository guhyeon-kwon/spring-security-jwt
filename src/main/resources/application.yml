spring:
  datasource:
    url: jdbc:mysql://localhost:3306/MySQL
    username: root
    password: Fj@P6L87x$al
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
